// ignore_for_file: prefer_const_constructors

import 'package:capabilites/capabilites.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Capabilites', () {
    test('can be instantiated', () {
      expect(Capabilites(), isNotNull);
    });
  });
}
