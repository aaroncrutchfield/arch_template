# Generated by mason_template_generator

name: app_template
description: A brick to create a new Flutter application based on {{project_name.snakeCase}}
version: 0.1.0+1
environment:
  mason: ">=0.1.0 <2.0.0"
vars:
  project_name:
    type: string
    description: The project name
    prompt: What is the project name?
  org_name:
    type: string
    description: The organization name (reverse domain notation)
    prompt: What is the organization name (com.example)?
    default: com.example
